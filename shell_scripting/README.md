# Shell Scripting

**Anuska Roy** — 24BCS10021

## Running

```bash
chmod +x *.sh
./hello.sh
```

`hello.sh` creates a `hello/` directory and `task1.sh` creates an `output/`
directory in the current working directory.